# Vedic Texts for RUDRA AI

This folder contains local Vedic and scripture summaries that the assistant uses when giving spiritual advice.

Files are loaded automatically by `backend/app/services/scripture_service.py` and included as additional system context during chat generation.

Add or replace files with your own local scripture texts, translations, or summaries. Supported formats are UTF-8 `.txt` files.
